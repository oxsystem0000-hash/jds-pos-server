const express=require('express');const{Low}=require('lowdb');const{JSONFile}=require('lowdb/node');const cors=require('cors');const path=require('path');const app=express();const PORT=process.env.PORT||3000;app.use(cors());app.use(express.json());const file=path.join(__dirname,'db.json');const adapter=new JSONFile(file);const db=new Low(adapter);async function init(){await db.read();db.data||={items:[]};await db.write()}init();app.get('/api/items',async(req,res)=>{await db.read();res.json(db.data.items.filter(i=>i.archived!=1).slice(0,200))});app.post('/api/item/archive',async(req,res)=>{const{bc,status}=req.body;await db.read();let item=db.data.items.find(x=>x.bc==bc);if(item){item.archived=status;await db.write();res.json({ok:true})}else res.json({ok:false})});app.listen(PORT,()=>console.log(PORT))